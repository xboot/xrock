# ready

Query whether the chip is in the ready state.

## Usage

```shell
xrock ready
```

## Description

Confirms whether the device in loader mode is ready to accept further commands.

## Example

```shell
xrock ready
```

When ready:

```
The chip is ready
```

Otherwise:

```
Failed to show chip ready status
```
